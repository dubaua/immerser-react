all files follow kebab-case naming, excluding react component files, that uses PascalCase naming

denied barrel export

all Props types sits in component file and do not exports without need

not end file export, export on place
